# Changelog

## 0.1.1

- Avoid `as const` in shared verdict type declarations so git dependency prepack builds remain compatible with consumer CI DTS parsing.

## 0.1.0

- Initial public extraction of the Electron live annotation capture workflow.
- Added renderer annotation mode, preload IPC bridge, main-process capture persistence, and local manifest receipts.
- Hardened capture authorization docs, renderer lifecycle behavior, persistence validation, and package verification before the TrySquadInc handoff.
